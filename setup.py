
# to activate the server_side's __init__.py across all the project 
 
from setuptools import setup, find_packages

setup(
    name='ai-workflow-capstone',
    version='1.0',
    packages=find_packages(),
)
